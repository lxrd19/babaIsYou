#ifndef DIRECTION_H
#define DIRECTION_H

/**
 * @brief The Direction enum
 * The direction where the actual item of the game can be pushed
 */
enum class Direction{
    RIGHT,
    UP,
    LEFT,
    DOWN
};

#endif // DIRECTION_H
